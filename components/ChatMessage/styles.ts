import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 5,
    },
    messageBox: {
        borderRadius: 10,
        padding: 5,
    },
    name: {
        color: "#00BFFF",
        fontWeight: 'bold',
        fontSize: 15,
    },
    message: {
        fontSize: 15,
    },
});

export default styles;